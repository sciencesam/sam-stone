  <div class='col-md-4'>
<% @posts.each do |post| %>
      <%= link_to(@post) do %>
        <div class= "panel panel-default">
          <div class= "panel-body">
            <div class="crop"><%= link_to image_tag(post.image.url, class: 'img-fluid'), post.image.url, target: '_blank' %></div>
          </div>
          <div class="panel-footer clearfix">
            <div class="panelFooty">
            <h3><%= post.title %></h3>
             <div class="card-content">
                <p><%= raw(post.abstract).truncate(358) %></p>
                <h6><%= post.created_at.strftime('%b %d, %Y') %></h6>
                <h6>Read more...</h6>
                <% end %>
                <% if admin_signed_in? %>
        <%= link_to "Show", post_path(post), class: "btn btn-primary" %>
        <%= link_to "Edit", edit_post_path(post), class: "btn btn-default" %>
        <%= link_to "Delete", post_path(post), class: "btn btn-danger", data: {:confirm => "Are you sure?"}, method: :delete %>
      <% end %>
              </div>
          </div>
        </div>
     
<% end %>
 </div>